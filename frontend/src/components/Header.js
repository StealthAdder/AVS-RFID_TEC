import Button from "./Button";

const Header = () => {
    return (
        <header className="header">
            <h1>Manufacturer Console</h1>
            <Button text="Add" />
        </header>
    );
};

export default Header;
